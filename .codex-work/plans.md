# 当前任务：右侧 Todo / iPhone 风格备忘录切换

- [x] 检查现有右侧 Todo、快速记录和 Electron/Vite 结构
- [x] 创建改动前 Git 备份提交
- [x] 实现 Todo / 备忘录可访问 Tab 切换
- [x] 实现备忘录列表、标题正文编辑、自动保存和删除
- [x] 为 Todo、备忘录、专注任务和专注记录接入 localStorage
- [x] 补充桌面端与窄窗口响应式样式
- [x] 运行 `npm run build`、`git diff --check` 并完成代码复核

## 中间任务列表圆点配色

- [x] 确认中间任务列表使用 `.central-task-mark` 圆形标记。
- [x] 按任务列表顺序添加蓝、黄、绿、红四种循环颜色类。
- [x] 使用浅色边框保持现有米白色界面的低饱和视觉风格。
- [x] 运行 `npm run build` 和 `git diff --check`。
- [x] 完成改动后的代码复核。

## 计时闭环与顺滑体验优化

- [x] 创建改动前 Git 备份提交 `ff75bf6`。
- [x] 增加 Electron 安全通知桥接和前端提示音/通知设置持久化。
- [x] 增加真实时间计时快照、刷新/重新打开后的恢复逻辑和独立快速记录持久化。
- [x] 完成专注/休息阶段自动切换、跳过状态、完成提示和正计时不循环进度。
- [x] 增加 Space/R/S/Escape 快捷键和移动端菜单行为。
- [x] 动态生成日期、校验自定义统计日期范围、补充 TimerView 类型和按钮无障碍标签。
- [x] 运行 `npm run build`、`git diff --check` 并完成运行态检查和代码复核。

## 任务卡片与自动计时交互

- [x] 将任务列表改为蓝、黄、绿、红四种浅色卡片背景。
- [x] 点击任务后自动启动该任务当前选择的计时模式。
- [x] 增加“退出任务”入口，并在退出时停止计时、保存已进行的专注记录。
- [x] 运行 `npm run build` 和 `git diff --check`。
- [x] 完成改动后的代码复核。

## Todo 专属想法记录

- [x] 为 Todo 增加兼容旧数据的专属想法数据结构。
- [x] 点击 Todo 展开历史想法，并支持新增想法。
- [x] 调整右侧 Todo 面板样式与交互状态。
- [x] 运行构建、空白检查并完成改动后的代码复核。

## Windows 图标与项目整理

- [x] 统一 Electron 运行时和 electron-builder 的番茄图标资源路径。
- [x] 增加标准格式化与格式检查命令，并格式化项目源码和配置。
- [ ] 清理历史临时打包目录 `release-0.1.1`、`release-final`、`release-rebuild`（其中 `app.asar` 被外部进程锁定，待进程退出后清理）；保留标准 `release/`（有正在运行的应用进程）和当前 `dist/`。
- [x] 在 README 和项目备忘中说明使用 `npm run dev` 加载最新源码，使用 `npm run electron:build` 更新独立桌面包。
- [x] 运行 `npm run format:check`、`npm run build` 和 `git diff --check`；Electron 安装包验证此前因 GitHub `winCodeSign` 下载连接被重置而中断。
- [x] 完成改动后的代码复核并记录验证结果。

## 专注统计按钮与自定义标语

- [x] 将左侧专注统计入口调整为独立强调按钮，保持原有跳转行为。
- [x] 增加首页标语点击编辑、保存、取消及恢复默认交互，支持换行。
- [x] 将标语保存在 `momofocus.slogan`，空白编辑内容不覆盖已保存标语。
- [x] 更新项目备忘并完成改动后的代码复核。
- [x] 运行 `npm run build`、`npm run format:check` 和 `git diff --check`。

## 任务详情、手动启动与任务颜色

- [x] 任务卡详情点击与明确开始按钮分离，当前任务也可从标题打开详情。
- [x] 展示任务专注次数、累计时长和放弃次数；新专注记录保存任务 ID，兼容旧记录并支持改名同步。
- [x] 增加任务名称/计时类型编辑、浅色预设选择和应用内删除二次确认。
- [x] 删除当前任务时停止计时并清理选中状态；旧任务颜色缺省时使用固定浅绿。
- [x] 完成浏览器交互复核及 code review。
- [x] 运行 `npm run format:check`、`npm run build` 和 `git diff --check`。

## 固定番茄钟运行流程与任务时长

- [x] 创建改动前 Git 备份提交 `7bfb9be`。
- [x] 为专注任务增加独立专注/休息分钟数，旧任务兼容为 25/5。
- [x] 将番茄钟固定为专注自动进入休息、休息结束后才计为完成。
- [x] 移除运行时阶段选择、重置、跳过和退出入口，只保留暂停/继续与放弃。
- [x] 调整暂停、放弃、完成历史记录规则，少于 5 秒不写入历史并显示提示。
- [x] 保留声音开关并在专注结束、休息结束时播放提示音；扩展计时快照保存会话开始时间。
- [x] 完成创建任务、自定义时长、启动、暂停和放弃的浏览器交互检查。
- [x] 运行 `npm run format:check`、`npm run build` 和 `git diff --check`。
- [x] 完成改动后的代码复核。

## 按任务统计时长分布

- [x] 将时长分布从单次会话时长区间改为不同任务的累计专注时长占比。
- [x] 兼容带任务 ID 和旧数据按任务名匹配的专注记录，并优化多任务图例显示。
- [x] 运行格式检查、构建、空白检查并完成改动后的代码复核。
